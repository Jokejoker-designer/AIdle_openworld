# Grok autonomous-phase kickoff prompt 001 (consolidated)

Paste the block below into the Grok Desktop coordinator session
(`019f7ffd-3995-71c0-aca1-51078e24a852`). It confirms the C5 foundation is now
Human-accepted, opens the autonomous phase (Directive 99), and answers Grok's
A/B/C question (the Human chose to close C5 first, then self-run).

---

```
C5 FOUNDATION ACCEPTED — AUTONOMOUS PHASE OPEN (read all, then run)

Update to your last status report: your A/B/C analysis was correct — the C5
foundation had to close before the autonomous phase. The Human Product Lead has
now done exactly that. Proceed:

1. Read IN FULL, end to end:
   - orchestration/receipts/ucbv_001/correction_010/HUMAN_ACCEPT_c5_foundation_001.json
     (Human ACCEPTED the UCBV-001 character+block visual FOUNDATION with
     residuals, under the Codex-absent capsule; this CLOSES Directive 98's C5
     milestone. It is a FOUNDATION accept only — NOT a full ship release, NOT a
     Nori visual sign-off, NOT a horizon unlock.)
   - orchestration/control/codex_directive.json  (Directive 99, now LIVE)
   - orchestration/control/AIDLE_COMMERCIAL_READY_MASTER_BRIEFING_001.md
   - orchestration/control/AIDLE_GAME_VISION_LOCK_001.md
   - AGENTS.md and orchestration/ARCHITECTURE_LOCK.md
   - latest orchestration/control/CONDUCTOR_JOURNAL.md entries (066-070)

2. Adopt Directive 99. Note the state moved 98 -> 99; refresh your live-control
   view from codex_directive.json (do not run off the old "98 IN_PROGRESS"
   snapshot). The three scope tiers:
   - TIER 1 (EXECUTE NOW, autonomous, no per-step check-in): Nori-7 visual
     redesign + its 5 deferred gardener clips (real keys); the C5H1 minor UX
     residuals incl. HUD "press E" -> KEY_C label fix; a post-UCBV self-run
     playbook (read/run/report the play loop, zero-error headed evidence, no
     product patch beyond a Human-flagged blocker); commercial-hardening
     VERIFICATION on the accepted foundation. You hold delegated narrow Godot
     override authority for TIER 1 ONLY — each wave must NAME the exact game/**
     file(s) in its WO + receipt, one writer per file, no Confirm-gate change.
   - TIER 2 (DRAFT then batch sign-off): Control 1B, Character Foundry 1C,
     remaining P1E art waves, and any hardening product patches. Draft a
     directive + WO (accepted=false) and QUEUE it in the next batch; execute
     only after the Human batch-approves.
   - TIER 3 (LOCKED, needs a dedicated directive): P2E-002, character-backbone
     program, DNA Platform vNext EXECUTION, and all H2-H6 horizons + TTS/voxel/
     neural/Text-to-3D. Do not open; escalate.

3. Operating rules (Directive 99):
   - Run TIER 1 continuously without check-ins. Escalate ONLY when: out-of-plan
     event, a Red F01 hard stop, an uncovered decision, a vision-lock conflict,
     or three identical failures (NEED_HUMAN).
   - Context discipline: at ~75% context, checkpoint state to durable files
     (journal + state notes + receipts), never hidden chat memory. After ANY
     compaction, re-read the six governance files in
     autonomous_operating_rules.context_discipline.after_compaction_must_reread_full
     BEFORE resuming.
   - Batch acceptance: accumulate Purple-VERIFIED waves + your independent-verify
     notes + any TIER 2 directive drafts, and present them as gate-ready BATCHES
     for the Human. No self-accept. Human is sole acceptor while Codex is out;
     Codex may re-assert on return — do not re-litigate given acceptances.
   - Every wave: schema-valid MAF receipt, real UUID lineage, accepted=false /
     self_accept=false, executable evidence, UTF-16LE logs decoded, hashes
     recorded. Documentation != implementation; green smoke != Human visual
     accept; metadata != animation.

4. First batch to run now (TIER 1): (a) Nori-7 visual redesign kickoff wave, (b)
   the HUD E/C label fix, (c) the post-UCBV self-run playbook read+run. Draft the
   TIER 2 directives (Control 1B, Foundry 1C, P1E) in parallel and queue them.

5. Acknowledge with: (a) confirmation you read all files incl. the C5 foundation
   acceptance, (b) the live directive_id (should read 99), (c) your proposed
   first TIER 1 batch + which TIER 2 directives you will draft, (d) confirmation
   you will escalate only out-of-plan events. Then begin.

Boundaries that never move: one writer per file; parent-only spawn, no
grandchildren, no second parent/CLI; quarantine + named-file Godot-override
discipline; TIER 3 stays locked; Red F01 (ship/publish/deploy/network/pay) is a
hard stop needing an explicit Human gate. Build to sellable quality; the act of
shipping is a separate Human decision.
```
