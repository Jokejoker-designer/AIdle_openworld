# DRAFT DIRECTIVE — Control 1B (TIER 2 — NOT AUTHORIZED TO EXECUTE)

Status: `DRAFT` · `accepted=false` · `self_accept=false`
Directive parent would supersede only after **Human batch sign-off**.
Task family: Control 1B (input / control / accessibility)
Related prior WOs (reference only): `WO-CTRL-1B-001-*`, `WO-CTRL-1B-002-*`

## Intent

Complete Control 1B to ship-quality: context-aware action catalog, accessibility
(color-independent state, reduced-motion, hide-aura), binding manager polish,
and residual R-C5H1-02/03 if still open after TIER 1.

## Proposed scope (for Human approval)

- Exact file lease TBD at batch approval (must name every `game/**` path).
- No Confirm-gate change without explicit Human line item.
- MAF: Schema → Blue → Red → QA → Purple → WAITING_HUMAN batch.

## Does NOT authorize

Execution, product patch, or network/ship. Queue only until Human batch-approves.
