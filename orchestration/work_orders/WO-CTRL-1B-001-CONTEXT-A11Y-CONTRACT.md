# WO-CTRL-1B-001-CONTEXT-A11Y-CONTRACT

Authority: `READ_ONLY_AUDIT` + scoped `PATCH_DRAFT` | State: `READY`

Control 1B begins only with contract and accessibility discovery. No Godot
product implementation is authorized in this work order.

## Sources and locked scope

- `Control/AIdle Keyboard & Mouse Control Blueprint.docx`
- `Control/CONTROL_IMPLEMENTATION_MAP.md`
- `Scene/SCENE_IMPLEMENTATION_TRACKER.md`
- Blueprint v1.1, Architecture Lock and current Godot InputMap/UI sources
- Private Reality 2.5D, Companion text-only, existing proposal -> preview ->
  explicit confirmation -> World Commit authority boundary

The common foundation must define Exploration, Companion, Build, Inspect and
World Tool contexts; stable InputMap actions; `/`, Ctrl+Enter, Tab, V/B,
Delete Proposal and Ctrl+Z compensation; context HUD of at most four actions;
remap, left/one-hand presets, hold/toggle, mouse sensitivity, reduced motion,
cursor size and configurable confirmation hold. No direct delete or durable
mutation is allowed.

## A0 - Existing-control and accessibility audit

Profile: `support-control-a11y`; TrustLayer: `code-reader`; UI:
`ui-a11y-auditor`; authority: `READ_ONLY_AUDIT`.

Read the actual InputMap, player/controller, HUD, Companion, manifestation,
proposal/confirm/cancel and focus code. Produce a source-line and runtime-backed
gap matrix for the five contexts, conflicts, focus trap, discoverability,
non-color cues, reduced motion and remapping. Do not patch product or contract.

Exclusive writes:

- `orchestration/receipts/control/CTRL_1B_001_a0_a11y_001.json`
- `orchestration/logs/ctrl-1b-001-a0-a11y-001.log`

## S1 - Falsifiable Control 1B contract

Profile: `schema`; TrustLayer: `devil-advocate`; UI: `ui-brief-writer`;
authority: scoped `PATCH_DRAFT` for contract files only.

Read A0 and turn the blueprint into versioned, fail-closed, testable contracts.
Unknown context/action IDs fail. One physical input cannot fire actions from
multiple active contexts. Delete and undo are proposal/compensation requests.
Preview cannot own collision/state. Esc cancel priority and Build-only R rotation
must be explicit. Include valid and invalid fixtures and a headed acceptance
checklist for later Blue implementation.

Exclusive contract writes:

- `Control/CONTROL_1B_ACCEPTANCE_CONTRACT.md`
- `orchestration/contracts/control_1b/input_context.schema.json`
- `orchestration/contracts/control_1b/valid_context_fixture.json`
- `orchestration/contracts/control_1b/invalid_context_fixture.json`

Exclusive evidence writes:

- `orchestration/receipts/control/CTRL_1B_001_s1_schema_001.json`
- `orchestration/logs/ctrl-1b-001-s1-schema-001.log`

## Workflow and acceptance

- Use only parent `019f7ffd-3995-71c0-aca1-51078e24a852`, coordinator-only.
- Exactly two real installed children, A0 then S1, one active at a time.
- No resume/follow-up child, grandchild or support profile beyond the assigned
  A0 profile.
- Load exact profiles, character cards, all five mandatory skills and routed
  skills fully. One writer per file. Receipts must validate against the MAF
  schema and remain `accepted=false`, `self_accept=false`.
- Parent returns `WAITING_CODEX`; Codex reviews before `CTRL-1B-002` product
  implementation is released.

## Hard stops

No Godot product/test/scene patch, Character Foundry, World 2, network,
dependency install, Godot version change, credential, live provider, push,
deploy or publish. Red F01 remains a hard network/shipping blocker.
