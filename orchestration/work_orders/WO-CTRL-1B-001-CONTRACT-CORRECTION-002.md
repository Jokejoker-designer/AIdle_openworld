# WO-CTRL-1B-001-CONTRACT-CORRECTION-002

Authority: scoped `PATCH_DRAFT` | State: `READY`

This correction closes only the independent Codex findings in
`orchestration/reviews/CODEX_CTRL-1B-001_CONTRACT_REVIEW_001.json`. It does not
start `CTRL-1B-002` and authorizes no Godot product, test, scene or runtime work.

## C0 - Schema and evidence correction

Profile: `schema`; TrustLayer: `devil-advocate`; UI: `ui-brief-writer`;
authority: scoped `PATCH_DRAFT` for the exact files below.

Use one fresh real Desktop child under parent
`019f7ffd-3995-71c0-aca1-51078e24a852`. Do not resume or follow up either
Directive-56 child. Load the profile, exact character cards, all five mandatory
skills and routed schema/design skills fully. No grandchild or support profile.

### Required corrections

1. Preserve the two original Directive-56 receipts and logs byte-for-byte.
2. Record the final durable `meta.json` SHA-256, parent, status, started_at and
   completed_at for A0 `019f869a-55bb-7210-b2e7-4396cb276a41` and S1
   `019f869f-21c3-7513-9d70-c91361eae08a` in the new correction receipt.
3. Harden the contract validator so the document contains exactly one of each
   context ID: exploration, companion, build, inspect and world_tool.
4. Add a deterministic executable fixture validator. It must validate the
   valid document and execute each of the twelve invalid payloads against its
   intended root/subschema or semantic rule. Rejecting only the invalid-suite
   wrapper is forbidden.
5. INV-08 must fail because one physical R dispatches camera and build rotation
   without context isolation. INV-09 must fail because pause precedes cancel.
   Every case must report its case_id, declared reason and PASS/FAIL.
6. Run both original receipts through
   `E:/standards/maf/schemas/agent_step_contract.schema.json`, then validate the
   new correction receipt through the same schema.

### Exclusive writes

- `Control/CONTROL_1B_ACCEPTANCE_CONTRACT.md`
- `orchestration/contracts/control_1b/input_context.schema.json`
- `orchestration/contracts/control_1b/invalid_context_fixture.json`
- `orchestration/contracts/control_1b/validate_control_1b_fixtures.py`
- `orchestration/receipts/control/CTRL_1B_001_c0_schema_correction_002.json`
- `orchestration/logs/ctrl-1b-001-c0-schema-correction-002.log`

`valid_context_fixture.json` is read-only and must remain at SHA-256
`d707bb32c43c005948599987fffa782d9206fb6ac2e04a623820d8b58427200a`.
The original A0/S1 receipts and logs are read-only historical evidence.

## Acceptance evidence

- One fresh child only; exact durable transcript/meta lineage.
- All three receipts validate against the MAF step schema.
- Valid fixture passes; exactly twelve invalid cases reject for their intended
  schema or semantic reasons; harness exits nonzero on any unexpected pass.
- Duplicate/missing context IDs fail closed.
- Contract documents the executable validator and no longer cites suite-root
  rejection as invalid-case proof.
- Writer lease and file hashes are exact; no undeclared temp.
- `accepted=false`, `self_accept=false`, `product_writes=[]` because these are
  governance contracts, not Godot product implementation.
- Parent returns `WAITING_CODEX`; only Codex may accept `CTRL-1B-001` or release
  `CTRL-1B-002`.

## Hard stops

No `game/**`, Character Foundry, Scene runtime, World 2, network, dependency
install, Godot version change, credential, live provider, push, deploy or
publish. Red F01 remains a hard stop before networked or shipping work.
