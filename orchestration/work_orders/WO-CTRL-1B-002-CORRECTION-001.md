# WO-CTRL-1B-002-CORRECTION-001

Authority: `PATCH_DRAFT` + `VERIFY_ONLY` | State: `READY`

Correct only the independently confirmed blockers from Directive 58. Preserve
the accepted Control 1B contract, all original Directive-58 receipts/logs and
all unrelated product work. This correction does not authorize Character
Foundry, World 2, persistence-format, World Commit authority or network work.

## Required outcomes

Close H-03, H-07, H-12, H-17, H-19, H-20, H-26 and H-28 plus A3-F09 and
A3-F10. The corrected headed matrix must be 33/33 PASS at 1280x720 and 868x517
with no clipping/overlap. In particular:

- Ctrl+Enter sends; Shift+Enter inserts a newline and never sends.
- Inspect opens a read-only provenance surface and performs no mutation.
- Q rotates left and R rotates right only in Build context.
- Companion understanding leads to a structured Proposal Card, never a direct
  mutation.
- confirmation-hold 0 is immediate; configured hold >=0.8 seconds cannot
  confirm early and is not limited to the demo harness.
- confirm emits only the existing validated commit handoff; cancellation before
  completion leaves no durable entity/collision. Collision activates only at
  complete. Do not add or simulate client-owned canonical World Commit.
- camera sensitivity has observable bounded effect.
- cursor scale and action-label-near-cursor settings have real runtime
  consumers and remain readable at both viewports.
- the settings UI exposes the complete remappable Control 1B foundation catalog.

## C0 - Runtime correction

Profile: `aidle-worldgen-godot-runtime`; TrustLayer:
`blue-team-p0-remediator`; UI: `ui-app-dashboard`; authority: `PATCH_DRAFT`.

C0 is the sole product/test writer. Modify only files needed from this lease:

- `game/scripts/main/main.gd`
- `game/scripts/player/player_controller.gd`
- `game/scripts/camera/cozy_camera.gd`
- `game/scripts/modules/companion/companion_chat_panel.gd`
- `game/scripts/modules/executor/headed_demo_flow.gd`
- `game/scripts/modules/manifestation/manifestation_instance.gd`
- `game/scripts/ui/control_settings_panel.gd`
- `game/scripts/ui/context_action_hud.gd`
- `game/scripts/ui/hud.gd`
- `game/scenes/ui/hud.tscn`
- `game/scenes/ui/control_settings_panel.tscn`
- `game/tests/control_1b_integration_smoke.gd`
- `game/tests/control_1b_headed_smoke.gd`

New read-only Proposal Card / inspect / cursor-label scripts or scenes are
allowed only under `game/scripts/ui/` and `game/scenes/ui/` with names prefixed
`control_1b_`. No other product/test path is leased.

Exclusive evidence:

- `orchestration/receipts/control/CTRL_1B_002_c0_runtime_correction_002.json`
- `orchestration/logs/ctrl-1b-002-c0-runtime-correction-002.log`

## Q1 - Fresh independent evidence

Profile: `aidle-worldgen-qa-evidence`; TrustLayer:
`purple-team-finding-triage`; UI: `ui-a11y-auditor`; authority: `VERIFY_ONLY`.

Run the contract validator, all Control smokes, clean boot and affected G8/P1E
regressions with zero ERROR/parse lines. Rebuild evidence under
`orchestration/evidence/control_1b_002_correction_001/**`. Prove all 33 H rows,
the ten correction findings, distinct hashes and real runtime states at both
viewports. Missing or scaffold-only evidence is FAIL.

Exclusive evidence:

- `orchestration/evidence/control_1b_002_correction_001/**`
- `orchestration/receipts/control/CTRL_1B_002_q1_qa_correction_002.json`
- `orchestration/logs/ctrl-1b-002-q1-qa-correction-002.log`

## A2 - Accessibility/safety re-audit

Profile: `support-control-a11y`; TrustLayer: `code-reader`; UI:
`ui-a11y-auditor`; authority: `READ_ONLY_AUDIT`.

Re-audit source and Q1 evidence. Findings only; never patch.

Exclusive evidence:

- `orchestration/receipts/control/CTRL_1B_002_a2_a11y_correction_002.json`
- `orchestration/logs/ctrl-1b-002-a2-a11y-correction-002.log`

## P3 - Purple correction gate

Profile: `aidle-worldgen-purple-acceptance`; TrustLayer:
`purple-team-release-gate`; UI: `ui-visual-critic`; authority: `VERIFY_ONLY`.

Verify product safety, 33/33 headed gates, responsive authenticity, regressions,
leases and lineage. Purple never patches or accepts.

Exclusive evidence:

- `orchestration/receipts/control/CTRL_1B_002_p3_purple_correction_002.json`
- `orchestration/logs/ctrl-1b-002-p3-purple-correction-002.log`

## Workflow and provenance

- Use only Desktop parent `019f7ffd-3995-71c0-aca1-51078e24a852`, coordinator-only.
- Exactly four fresh real children C0 -> Q1 -> A2 -> P3, strictly sequential,
  one active child maximum. No resume/follow-up/grandchild/extra profile.
- Bind exact TrustLayer/UI characters and load all five mandatory skills plus
  routed skills fully.
- One writer per file. Parent never patches product, test or evidence.
- Original B1 and Q2 receipt lineage mismatches remain immutable historical
  evidence. Every correction receipt must identify its own real durable Grok
  session directory ID, record the two historical mismatches, and use
  `accepted=false`, `self_accept=false`, schema-valid MAF fields, exact commands,
  exits, hashes and writer lease. If the active durable directory cannot be
  identified uniquely, stop rather than invent a UUID.
- Parent returns `REVIEW_REQUESTED` / `WAITING_CODEX`. Only Codex may accept.

## Hard stops

No accepted-contract/fixture edit; no direct durable delete or undo; no client
canonical authority; no Character Foundry, Scene 2, World 2, persistence or
network; no dependency install, Godot version change, credential, live provider,
public listener, push, deploy or publish. Red F01 still blocks network/shipping.
