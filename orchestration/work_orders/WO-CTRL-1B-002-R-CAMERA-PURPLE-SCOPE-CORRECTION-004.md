# WO-CTRL-1B-002-R-CAMERA-PURPLE-SCOPE-CORRECTION-004

Authority: `VERIFY_ONLY` | State: `READY`

Codex independently confirms the Control 1B runtime and R-camera behavior pass.
Directive 61 E0 is schema-valid and accepted as correction evidence. Directive
61 E1 is not acceptance evidence because its durable transcript created and
deleted `orchestration/logs/_e1_write_receipt.py`, a real third output outside
the exact two-file lease, while its receipt claimed `exclusive_lease_only=true`.

This correction is provenance-only. It authorizes no product, test, contract,
fixture, screenshot, prior receipt, prior log, task, directive, Scene,
Character Foundry, DNA package, persistence, network or World Commit write.

## P0 - fresh Purple verification

Profile: `aidle-worldgen-purple-acceptance`; TrustLayer:
`purple-team-release-gate`; UI: `ui-visual-critic`; authority: `VERIFY_ONLY`.

Use one fresh real child under the existing parent. Re-run the fixture
validator, context-router smoke, integration smoke and witness hash. Validate
the accepted E0 receipt directly against
`E:/standards/maf/schemas/agent_step_contract.schema.json` using the already
available `jsonschema` package; do not install or repair anything.

Write exactly and only:

- `orchestration/receipts/control/CTRL_1B_002_p0_purple_scope_correction_005.json`
- `orchestration/logs/ctrl-1b-002-p0-purple-scope-correction-005.log`

The receipt must include top-level `smoke_test`, real durable child lineage,
exact commands and exits, the E0 hash, the rejected E1 transcript ref and
out-of-lease finding, `product_writes=[]`, `accepted=false`,
`self_accept=false`, and `next_route=WAITING_CODEX`.

## Exact write discipline

- Use the Grok `write` tool directly for the leased receipt and leased log.
- Do not create a helper, temporary, cache, scratch, patch, generated script or
  deleted intermediate anywhere in or outside the repository.
- All validation after the two writes must be read-only and inline; no command
  may redirect output to a file.
- The durable transcript must contain exactly two file-write tool calls, both
  to the leased paths above, and no filesystem delete/move/rename call.
- Purple verifies only and never accepts.

## Workflow and hard stops

- Same sole Desktop parent `019f7ffd-3995-71c0-aca1-51078e24a852`, coordinator-only.
- Exactly one fresh real child. No resume, follow-up, grandchild, support
  profile or extra session.
- Load the exact characters, five mandatory skills, `evidence-memory-ledger`
  and `adversarial-review` fully.
- No dependency install, Godot change, credential, live provider, public
  listener, push, deploy or publish.
- Parent returns `REVIEW_REQUESTED` / `WAITING_CODEX`, accepted=false.
