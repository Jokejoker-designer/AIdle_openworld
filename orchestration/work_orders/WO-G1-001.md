# Work Order WO-G1-001

| Field | Value |
|---|---|
| Task | G1-001 — Complete World Prompt fixtures and validator |
| Owner | aidle-schema |
| Authority | PATCH_DRAFT |
| Dependencies | G0-001 ACCEPTED |
| Product lock | 2.5D first; Companion text-only MVP |

## Goal

Make the Structured World Prompt contract executable: **10 valid fixtures accepted**, **10 invalid fixtures rejected**, with **format checks enabled** in the project validator.

## Allowed write paths

- `contracts/fixtures/valid/**`
- `contracts/fixtures/invalid/**`
- `contracts/world_prompt.schema.json` (only if a gap blocks acceptance; keep additionalProperties:false)
- `contracts/examples/**` (keep examples consistent if changed)
- `scripts/validate_project.py` (extend fixture + format checks only)
- `orchestration/receipts/G1-001.json`
- `orchestration/work_orders/WO-G1-001.md` (status updates only)

## Forbidden

- Do not implement Godot gameplay, Companion runtime, TTS/voice, or multiplayer.
- Do not touch `AIdle_Openworld_Blueprint_v1.0/` as authority (read only if needed).
- Do not install packages, push, deploy, or use paid APIs.
- Do not claim G1-002 scope.

## Acceptance tests

1. Exactly ≥10 JSON files under `contracts/fixtures/valid/` each **pass** Draft 2020-12 + FormatChecker against `world_prompt.schema.json`.
2. Exactly ≥10 JSON files under `contracts/fixtures/invalid/` each **fail** validation (diverse reasons: unknown field, reversed/wrong stages, bad UUID/date-time, custom style without ref, bounds, confirmation without confirmed_by, wrong schema_version, etc.).
3. `python scripts/validate_project.py` exits 0 and prints `AIDLE_VALIDATION=PASS`.
4. Validator must assert fixture counts (≥10 valid, ≥10 invalid) and enable format checks.
5. Emit `orchestration/receipts/G1-001.json` as agent_step_contract with smoke_test evidence.

## Deliverables

- Fixture sets + updated validator
- Receipt with command output summary
- `next_route` suggesting consumer review then G2 unlock only after ACCEPT

## Status

REVIEW_REQUESTED by aidle-schema · 2026-07-20

- Fixtures: 11 valid + 12 invalid under `contracts/fixtures/`
- Smoke: `python scripts/validate_project.py` → exit 0, `AIDLE_VALIDATION=PASS`
- Receipt: `orchestration/receipts/G1-001.json`
- Not self-accepted; awaiting independent review
