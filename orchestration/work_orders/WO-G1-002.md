# Work Order WO-G1-002

| Field | Value |
|---|---|
| Task | G1-002 — Lock commit authority and event contracts |
| Owner | aidle-network |
| Authority | PATCH_DRAFT |
| Dependencies | G0-001 ACCEPTED |
| Product lock | Server-authoritative durable state; 2.5D first |

## Goal

Codify commit authority so that **client durable mutation is rejected**, and **idempotency + revision conflict** are machine-checkable (schema + tests + policy docs under contracts).

## Allowed write paths

- `contracts/commit/**` (commit request, receipt, rejection, conflict schemas)
- `contracts/events/**` (event envelope + core event payload schemas as needed)
- `contracts/fixtures/commit/**` (positive/negative fixtures)
- `AIdle_Openworld_Blueprint_v1.1/Interfaces/**` only if clarifying non-contradicting machine path refs
- `scripts/validate_project.py` (add commit/event contract checks; do not break G1-001 checks)
- `orchestration/receipts/G1-002.json`
- `orchestration/work_orders/WO-G1-002.md` (status updates only)

## Forbidden

- Do not implement Nakama/Colyseus runtime or live multiplayer server.
- Do not add both Nakama and Colyseus as co-owners.
- Do not implement Godot client gameplay.
- Do not install packages, push, deploy, or use paid APIs.
- Do not rewrite world_prompt fixtures owned by G1-001 (schema worker owns those paths).

## Acceptance tests

1. JSON Schema(s) define:
   - Commit request path that only authoritative service may accept for durable mutation.
   - Explicit rejection for client-forged / client-authoritative durable mutation attempts.
   - Idempotency: same `request_id` returns prior receipt (schema + fixture).
   - Revision conflict: mismatched `expected_world_revision` yields conflict status (schema + fixture).
2. At least one **invalid** fixture where a client attempts durable mutation without server authority is **rejected**.
3. Event envelope matches `Interfaces/Event_Bus.md` fields: event_id, event_type, event_version, occurred_at, request_id, space_id, world_revision, actor_id, payload, trace_id.
4. `python scripts/validate_project.py` still exits 0 (coordinate: if concurrent with G1-001, add checks that tolerate missing G1-001 fixtures only if not yet present — prefer checks that pass when your fixtures exist).
5. Emit `orchestration/receipts/G1-002.json` agent_step_contract with smoke_test evidence.

## Deliverables

- Commit + event contracts + fixtures
- Validator coverage
- Receipt + next_route for consumer/purple review

## Status

REVIEW_REQUESTED · aidle-network PATCH_DRAFT · 2026-07-20

Receipt: `orchestration/receipts/G1-002.json`

Smoke: `python scripts/validate_project.py` → AIDLE_VALIDATION=PASS (exit 0)

