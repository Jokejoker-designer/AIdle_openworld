# Work Order WO-G2-003

| Field | Value |
|---|---|
| Task | G2-003 — Companion proposal + bounded personality boundary |
| Owner | aidle-companion |
| Authority | PATCH_DRAFT |
| Dependencies | G1-001 ACCEPTED |

## Goal

Text-only Companion: produces **schema-valid World Prompt proposals**, **no direct commit tool**, slow drift caps from `personality_profile.schema.json`, inspect/lock/reset/delete controls.

## Allowed write paths

- `game/scripts/modules/companion/**` (create)
- `game/scenes/modules/companion/**` (create)
- `game/scripts/modules/interfaces/i_companion_module.gd`
- `game/scripts/modules/stubs/**` only if companion stub replacement
- `game/scenes/ui/**` only for minimal text chat/proposal panel if needed (do not redesign whole HUD)
- `orchestration/receipts/G2-003.json`
- `orchestration/work_orders/WO-G2-003.md`

## Forbidden

- **No STT, TTS, voice cloning, microphone, audio models, voice dependencies.**
- No direct world commit / no SceneTree mutation of durable entities.
- Do not edit contracts schemas (consume them only).
- Do not own core camera/player files.

## Acceptance

1. Proposal output validates against `contracts/world_prompt.schema.json` (export JSON or fixture from companion path).
2. Tool surface has no commit/durable-mutate tool.
3. Adaptive trait deltas respect schema caps (max_delta_per_turn/day/distance).
4. Inspect, lock, reset, delete personality controls exist (API or UI text).
5. REVIEW_REQUESTED + receipt.

## Status

REVIEW_REQUESTED · 2026-07-20 · aidle-companion (PATCH_DRAFT) · receipt `orchestration/receipts/G2-003.json` · no self-ACCEPT

### Delivery notes

- Module: `CompanionModule` + `CompanionWorldPromptBuilder` + `CompanionPersonalityProfile`.
- Export: `game/scripts/modules/companion/exports/companion_proposal_cozy_house.json` (schema-valid, `confirmation.state=pending`).
- Caps: turn ≤ 0.005, day ≤ 0.03, distance ≤ 0.25; lock/reset/delete/inspect on API and `/inspect|/lock|/reset|/delete` text commands.
- Stub upgrade: `ModuleStub` companion slot loads real CompanionModule.
- Smoke: `python game/scripts/modules/companion/smoke_g2_003.py` → PASS; Godot `-s companion_headless_smoke.gd` → PASS.
