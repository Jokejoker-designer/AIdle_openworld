# Work Order WO-G2-006

| Field | Value |
|---|---|
| Task | G2-006 — Deterministic AGM Decision executor |
| Owner | aidle-executor |
| Authority | PATCH_DRAFT |
| Dependencies | G1-003 ACCEPTED |

## Goal

Execute allowlisted Decision Envelope actions only: dialogue → companion, builds → preview/commit path, events allowlist, mood/relationship deltas capped. Idempotent by decision_id. Produce execution receipt.

## Allowed paths

- `game/scripts/modules/executor/**` (create)
- `game/scripts/modules/interfaces/i_executor_module.gd`
- `game/scripts/modules/stubs/**` only if upgrading executor stub
- `orchestration/receipts/G2-006.json`
- this WO

## Acceptance

1. Unknown action types rejected.
2. decision_id idempotency (replay returns prior receipt).
3. Build actions never skip preview/confirm/commit contracts.
4. Execution receipt for next snapshot.
5. Smoke + REVIEW_REQUESTED.

## Forbidden

- No direct SceneTree durable mutation bypassing commit contracts.
- No paid provider SDKs (G5-001).
