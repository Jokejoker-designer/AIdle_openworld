# Work Order WO-G2-007

| Field | Value |
|---|---|
| Task | G2-007 — Free vs Paid edition selector |
| Owner | aidle-core |
| Authority | PATCH_DRAFT |
| Dependencies | G1-003 ACCEPTED |

## Goal

First-run edition choice persisted; same AGM contract semantics; paid path has **no client secret**.

## Allowed paths

- `game/scripts/core/**` edition settings only
- `game/autoload/settings_manager.gd` if needed for persistence
- `game/scenes/ui/**` first-run selector only
- `game/scripts/main/boot.gd` edition gate only
- `orchestration/receipts/G2-007.json`
- this WO

## Acceptance

1. User can choose desktop_bridge_free vs api_paid once (or change via settings with consent).
2. Both modes use same snapshot/decision schemas.
3. Paid mode stores no provider API key in Godot project (gateway later G5).
4. Smoke + REVIEW_REQUESTED.

## Forbidden

- No embedding API keys. No install packages.
