# WO-G8-001-D2-VERIFICATION-009

Directive: 29  
Task: G8-001  
State: CHANGES_REQUESTED  
Parent: existing Grok Desktop session `019f7ffd-3995-71c0-aca1-51078e24a852` only

## Release basis

Codex accepted D1 in
`orchestration/reviews/CODEX_G8-001_D1_FINALIZER_ACCEPTANCE_009.json`.
The accidental top-level task `019f8289-...`, cancelled child `019f828b-...`,
and all premature D2 outputs remain quarantined and are not evidence.

## Required D2 wave

The parent is coordinator-only. Spawn exactly four new real Desktop children
from installed profiles `companion`, `manifestation`, `asset`, and `persist`.
They may run concurrently with at most four active children. No grandchildren.
No child may patch product, tests, contracts, harnesses, screenshots, shared
logs, prior receipts or prior reviews.

| Profile | Authority | TrustLayer | UI character | Verification gate |
|---|---|---|---|---|
| companion | VERIFY_ONLY | blue-team-p0-remediator | ui-component-craftsman | text-only Companion, bounded personality, proposal-only/no commit, responsive panel |
| manifestation | VERIFY_ONLY | blue-team-p0-remediator | ui-component-craftsman | ordered four-stage manifestation, runtime-bound screenshots, cancel entity absent |
| asset | VERIFY_ONLY | blue-team-p0-remediator | ui-color-type-specialist | Dreamy Low-Poly Starter Realm, Cozy default, style/provenance consistency |
| persist | READ_ONLY_AUDIT | purple-team-finding-triage | ui-a11y-auditor | 1280x720 and 868x517 readability, keyboard/contrast audit, G4 evidence consistency |

Each child must read its full `.grok/agents/<profile>.md`, exact TrustLayer and
UI cards, `orchestration/skills_manifest.yaml`, this work order, WO-004, the UI
skill dispatch map and active `DESIGN.md`. Every child loads all five manifest
`always` skills in full: `maf-mandatory-standard`, `trustlayer-x16-crew`,
`agentwork-knowledge-loop`, `project-room-collab`, and `curiosity-engine`.
Routed skills are locked by WO-004: companion `game-ui-icons`; manifestation
`game-asset-core` plus `game-ui-icons`; asset `game-asset-core`; persist uses
the `ui-a11y-auditor` character rules. Do not claim catalog-only bundles ran.

## Evidence lease

Each child is sole writer only for its new receipt and one profile-prefixed
UTF-8 trace:

- `orchestration/receipts/g8/subagent_workflow_remediation_004/D2_companion.json`
- `orchestration/receipts/g8/subagent_workflow_remediation_004/D2_manifestation.json`
- `orchestration/receipts/g8/subagent_workflow_remediation_004/D2_asset.json`
- `orchestration/receipts/g8/subagent_workflow_remediation_004/D2_persist.json`

Every receipt validates against
`E:/standards/maf/schemas/agent_step_contract.schema.json` and contains its new
real `child_task_ref`, `spawned_by_parent_ref`, exact character and skill
sources/modes/load evidence, context hash, files read/written, literal
read-only commands and exits, trace/handoff, `product_writes=[]`, and
`self_accept=false`.

## Read-only gates

- Verify existing G3 `PASS checks=76`, G4 `PASS checks=22`, and six-export
  zero-diff evidence; do not rerun commands that mutate shared evidence.
- Verify `evidence_manifest.json` has ten distinct dimension-correct captures,
  zero error lines, runtime-bound stages and successful cancel proof.
- Companion remains text-only and has no World Commit tool.
- Free Bridge remains manual with no API, credential or hidden automation.

## Completion

After all four children finish, parent updates only `grok_status.json` and
returns `CHANGES_REQUESTED / WAITING_CODEX` with the four new child refs,
`parent_product_patch=false`, `d3_spawn_allowed=false`, and `accepted=false`.
Codex independently reviews D2 before releasing D3.

No G8 acceptance, D3, Control 1B, support profiles, new top-level session,
Grok CLI, install, push, deploy or publish.
