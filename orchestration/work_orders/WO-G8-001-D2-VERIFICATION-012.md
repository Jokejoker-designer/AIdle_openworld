# WO-G8-001-D2-VERIFICATION-012

Directive: 33  
Task: G8-001  
State: CHANGES_REQUESTED  
Parent: existing Grok Desktop session `019f7ffd-3995-71c0-aca1-51078e24a852` only

## Human-approved timing protocol

Human Product Lead authorized filesystem timestamps plus durable Grok child
metadata as canonical evidence. Workers must not predict their own durable
completion. Each worker records an honest receipt-writer time as informational;
after it exits, Codex binds `completed_at` from immutable child metadata and
artifact `LastWriteTimeUtc` plus SHA-256 in the acceptance review. Historical
receipts remain unchanged.

## Required D2 wave

The parent is coordinator-only. Spawn exactly four fresh real Desktop children
from installed profiles `companion`, `manifestation`, `asset`, and `persist`.
Maximum four active children. No grandchildren and no support profiles.

| Profile | Authority | TrustLayer | UI character | Verification gate |
|---|---|---|---|---|
| companion | VERIFY_ONLY | blue-team-p0-remediator | ui-component-craftsman | text-only Companion, bounded personality, proposal-only/no commit, responsive panel |
| manifestation | VERIFY_ONLY | blue-team-p0-remediator | ui-component-craftsman | four ordered stages, runtime-bound existing screenshots, cancel entity absent |
| asset | VERIFY_ONLY | blue-team-p0-remediator | ui-color-type-specialist | Dreamy Low-Poly Starter Realm, Cozy default, style/provenance consistency |
| persist | READ_ONLY_AUDIT | purple-team-finding-triage | ui-a11y-auditor | 1280x720 and 868x517 readability, keyboard/contrast, G4 evidence consistency |

Each child reads its full `.grok/agents/<profile>.md`, exact TrustLayer/UI cards,
`orchestration/skills_manifest.yaml`, this work order, WO-004, UI dispatch map,
and active `DESIGN.md`. Load in full all five manifest `always` skills:
`maf-mandatory-standard`, `trustlayer-x16-crew`,
`agentwork-knowledge-loop`, `project-room-collab`, and `curiosity-engine`.
Routed skills: companion `game-ui-icons`; manifestation `game-asset-core` plus
`game-ui-icons`; asset `game-asset-core`; persist uses its a11y character rules.

## New evidence lease

Premature D2 receipts remain quarantined. Fresh children write only:

- companion: `D2_companion_012.json` and `g8-d2-companion-012.log`
- manifestation: `D2_manifestation_012.json` and `g8-d2-manifestation-012.log`
- asset: `D2_asset_012.json` and `g8-d2-asset-012.log`
- persist: `D2_persist_012.json` and `g8-d2-persist-012.log`

Receipt directory:
`orchestration/receipts/g8/subagent_workflow_remediation_004/`  
Trace directory: `orchestration/logs/`

No product, test, contract, harness, screenshot, shared-log, prior-receipt or
prior-review write. No helper/temp file. Every receipt validates against the MAF
step schema and includes the fresh child ref, parent ref, exact character and
skill sources/modes/load evidence, context hash, complete files read/written,
literal commands with exits and tool-call refs, material non-terminal tools,
trace/handoff, `product_writes=[]`, and `self_accept=false`.

## Read-only gates

- Inspect existing G3 `PASS checks=76`, G4 `PASS checks=22`, and six-export
  zero-diff evidence without rerunning evidence-mutating commands.
- Inspect the existing headed manifest: ten distinct dimension-correct captures,
  zero error lines, runtime stages and cancel proof.
- Companion remains text-only with no World Commit tool.
- Free Bridge remains manual with no API, credential or hidden automation.

## Completion

After all four children finish, parent updates only `grok_status.json` and
returns `CHANGES_REQUESTED / WAITING_CODEX` with four fresh child refs,
`parent_product_patch=false`, `d3_spawn_allowed=false`, and `accepted=false`.
Codex independently validates transcript, scope, receipt schema and externally
binds timing before any D3 release.

No G8 acceptance, D3, Control 1B, new top-level session, Grok CLI, dependency
installation, push, deploy or publish.
