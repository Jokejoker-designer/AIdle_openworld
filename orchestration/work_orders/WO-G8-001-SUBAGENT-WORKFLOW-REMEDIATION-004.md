# WO-G8-001-SUBAGENT-WORKFLOW-REMEDIATION-004

Directive: 25  
Task: G8-001  
State on issue: CHANGES_REQUESTED  
Parent: the one existing Grok Desktop task whose title starts `Read AGENTS.md & CONDUCTOR_PROMPT.md`  
Scope: adopt and independently verify the preserved Directive 24 draft through real installed Grok child subagents.

## Why this correction exists

Directive 24 produced a technically promising draft, but the parent directly patched the work and the eight receipts do not contain durable child-task/transcript evidence. Naming a profile, skill or character in JSON is not equivalent to running that installed subagent. Preserve every current code/evidence file; repair the workflow and provenance in place.

## Non-negotiable orchestration

1. The parent is coordinator-only. It may read, spawn, route, collate and update `orchestration/control/grok_status.json`; it must not patch product, test, contract or evidence-harness files.
2. Use Grok Desktop `spawn_subagent` child tasks from the installed profiles under `.grok/agents`. Do not emulate workers in the parent prompt.
3. No child may spawn a grandchild. Maximum five active child tasks.
4. Execute these waves exactly:
   - D0: `schema` alone.
   - D1: `core` and `executor` concurrently after D0.
   - D2: `companion`, `manifestation`, `asset`, and `persist` concurrently after D1.
   - D3: `network` Purple gate alone after D2.
5. One writer owns each file. `core` and `executor` are the only PATCH_DRAFT product/test writers. All other profiles are VERIFY_ONLY or READ_ONLY for this correction.
6. Parent and workers may not self-ACCEPT. Return `REVIEW_REQUESTED / WAITING_CODEX`.

## Required profile, character and skill bindings

Each child must read its full installed `.grok/agents/<profile>.md`, the named TrustLayer/UI character cards, and the exact skill sources before acting.

| Child | Authority in this WO | TrustLayer character | UI character | Required skills and mode |
|---|---|---|---|---|
| schema | VERIFY_ONLY | devil-advocate | ui-brief-writer | maf-mandatory-standard full; od-design-brief full; od-reference-design-contract full |
| core | PATCH_DRAFT | blue-team-p0-remediator | ui-component-craftsman | maf-mandatory-standard full; game-ui-icons full; game-asset-core full |
| executor | PATCH_DRAFT | blue-team-test-writer | ui-component-craftsman | maf-mandatory-standard full; game-ui-icons full |
| companion | VERIFY_ONLY | blue-team-p0-remediator | ui-component-craftsman | maf-mandatory-standard full; game-ui-icons full |
| manifestation | VERIFY_ONLY | blue-team-p0-remediator | ui-component-craftsman | maf-mandatory-standard full; game-asset-core full; game-ui-icons full |
| asset | VERIFY_ONLY | blue-team-p0-remediator | ui-color-type-specialist | maf-mandatory-standard full; game-asset-core full |
| persist | READ_ONLY_AUDIT | purple-team-finding-triage | ui-a11y-auditor | maf-mandatory-standard full; ui-a11y-character-rules character_rules |
| network | VERIFY_ONLY | purple-team-release-gate | ui-visual-critic | maf-mandatory-standard full; od-impeccable-design-polish character_rules; od-design-review catalog_guidance only |

Use the exact source paths already locked by `orchestration/work_orders/G8-001_UI_SKILL_DISPATCH_MAP.md` and the U0-U7 receipts under `orchestration/receipts/g8/ui_skill_correction/`. Do not claim catalog-only bundles were fully executed.

## Writer ownership

- `core` is sole writer for `scripts/run_g8_headed_visual_c003.py`, `game/scripts/core/headed_visual_smoke.gd`, HUD/main/Bridge and art-style runtime files if a minimal correction is proven necessary.
- `executor` is sole writer for `game/scripts/modules/executor/headed_demo_flow.gd`, `game/scripts/modules/g3_ui/starter_realm_panel.gd` and its owned headed-flow assertions if a minimal correction is proven necessary.
- Any file outside those sets is read-only in this correction unless Codex issues another work order.
- If the preserved draft already passes a child-owned gate, the child records adoption and verification without rewriting the file.

## Required real-child evidence

Write replacement receipts only under `orchestration/receipts/g8/subagent_workflow_remediation_004/`. Each receipt must validate against `E:/standards/maf/schemas/agent_step_contract.schema.json` and include:

- non-empty `character_binding` and exact `skills_loaded` source/mode;
- `input_context_hash`, `trace_ref`, and `handoff_ref`;
- `child_task_ref`: the durable Grok Desktop child task/transcript identifier;
- `spawned_by_parent_ref`: the current parent task identifier;
- actual files read and written, commands, exit codes and timestamps;
- `self_accept: false` and no invented evidence.

The parent collate receipt must include `parent_product_patch: false`, the eight child task references and their wave order. Near-simultaneous hand-authored receipts without child transcript IDs fail this gate.

## Technical gates to re-establish

- canonical headed runner rejects any unexpected Godot ERROR;
- ten distinct, dimension-correct, runtime-state-bound screenshots and `evidence_manifest.json`;
- cancel proof shows preview entity/collision absent and a changed world crop;
- Bridge snapshot UUID is valid and manual consent/result state is visible;
- Cozy clean-world default and preserved explicit saved style are both honestly proven;
- readable, non-overlapping 1280x720 and 868x517 UI;
- manifestation, Companion, Bridge, validator, G3=76, G4=22 and six tracked export zero-diff gates remain green.

## Out of scope and hard stops

- Do not start Control Foundation 1B until this correction is accepted.
- No rollback, deletion, rebuild from scratch, new top-level Grok session, Grok CLI, dependency install, voice, live provider, credential, public listener, push, deploy or publish.
- Do not bypass schema, preview, consent, confirmation or World Commit.

## Completion route

The parent collates only after D3 and updates `grok_status.json` to `REVIEW_REQUESTED / WAITING_CODEX`. Codex independently checks the child transcripts, receipts, headed evidence and regressions before any acceptance.
