# Work Order — G8-001 Independent 2.5D Alpha Evidence Gate

Final machine acceptor: Codex. Final alpha acceptance: Human Product Lead.
Continue only in the existing Grok Desktop parent.

This work order is `VERIFY_ONLY`. No product patch is authorized. A discovered
failure must become `CHANGES_REQUESTED` with evidence; do not repair it inside
this gate.

## Eight installed domain subagents

Run all eight installed profiles, each with `VERIFY_ONLY` authority and its own
schema-valid `agent_step_contract`:

1. `schema`: contracts, fixtures, validator and receipt completeness.
2. `core`: fixed-angle 2.5D shell, edition selection and clean boot.
3. `manifestation`: wireframe → hologram → materializing → complete, cancel
   collision safety and preview-before-confirm.
4. `companion`: text-only AGM dialogue/personality boundary, no commit tool.
5. `asset`: starter house grammar, style tokens, provenance and licenses.
6. `executor`: deterministic Decision execution and G3 onboarding transaction.
7. `persist`: signed journal, save/reload, replay, compensation and G4 integrity.
8. `network`: Paid gateway fixture boundary plus G6 local authority/convergence.

Profiles may write only their new G8 receipt files. Parent may write the G8
dispatch map, collated receipt, alpha evidence report and `grok_status.json`.
No profile may modify product/tests/contracts/tasks/directive/architecture or
prior evidence. One writer per evidence file, no nested grandchildren, no
self-acceptance.

## Integrated machine gate

Independently rerun and record, at minimum:

- project validator;
- clean integrated Godot boot and fixed-angle 2.5D shell smoke;
- manifestation smoke;
- Companion AGM/text-only smoke;
- Free Desktop Bridge and edition smokes;
- deterministic AGM executor and full G3 prompt-to-house E2E;
- G4 signed persistence smoke;
- G5 gateway suite and Paid adapter smoke;
- G6 authority suite and Godot two-client smoke;
- secret, dependency, public-bind and forbidden-scope scans;
- all task worker receipts and Codex acceptance reviews from G0 through G6;
- all G8 step receipts against the canonical MAF schema.

The report must distinguish:

- executable proof;
- documentation-only claims;
- local POC limitations;
- deferred roadmap items (voice, free-form 3D, voxel terrain, real cities,
  production multiplayer, marketplace/economy and live AI provider).

Do not claim a visual/commercial/production alpha solely from headless tests.
Prepare a concise Human Acceptance Checklist for launching the local Godot
build and manually confirming presentation, movement, prompt flow, Companion
text, manifestation stages, cancel behavior, save/reload and edition choice.

## Completion state

If any machine gate fails: return `REVIEW_REQUESTED` with Purple
`CHANGES_REQUESTED` evidence.

If every machine gate passes: return `HITL_REQUIRED`, not `ACCEPTED`, with:

- machine verdict `PASS_FOR_HUMAN_REVIEW`;
- evidence index and remaining risks;
- Human Acceptance Checklist;
- no task-state edit.

No install, dependency repair, live provider/network, credential, push, deploy
or publish. Wait for Codex after updating only `grok_status.json`.
